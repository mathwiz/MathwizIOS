package pattern.behavioral

class StringLength extends Magnitude {
    def gt(other) {
        val ?.size() > other.val ?.size()
    }

    boolean equals(other) {
        this.val ?.size() == other.val ?.size()
    }
}
